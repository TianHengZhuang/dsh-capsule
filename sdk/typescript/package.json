{
  "name": "@dsh-capsule/extension-sdk",
  "version": "0.1.0",
  "description": "DSH Capability Guard Managed Extension SDK (types & helpers only)",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5.6",
    "vitest": "^2.1"
  }
}
