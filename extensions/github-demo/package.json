{
  "name": "@dsh-capsule/github-demo",
  "version": "0.1.0",
  "description": "DSH Capability Guard Managed Extension demo (GitHub issues via Broker)",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@dsh-capsule/extension-sdk": "workspace:*"
  },
  "devDependencies": {
    "@dsh-capsule/adapter": "workspace:*",
    "@types/node": "^22",
    "typescript": "^5.6",
    "vitest": "^2.1"
  }
}
